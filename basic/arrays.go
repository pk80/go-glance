package basic

/*
Array : is a fixed-length sequence that is used to store homogenoues(same type) elements in the memory.
*/
func ArrayDataTypes() {
	// one-dimensional array :
	// syntax : var array_name[length]Type
	// syntax : var array_name[length] = elelment
	// approach 1 : array_name := [length]Type{item1,item2,...,itemN}
	// approach 2 :
	// multi-dimensional array :
	// syntax : var array_name[len1][len2]...[lenN]Type
	// default value of an array is 0 if not initialized explicitly
	// length of an array : len(array_name)
	// ellipsis : array_name := [...]Type{item1,item2,...,itemN}
	// range over an array : for loop
	// comparable : arr1 == arr2
}
